## 0.1.0

- Initial extracted snapshot: `KeyedFormController` (draft, field-keyed
  errors, touched/dirty/revealed bookkeeping), `KeyedFormMode`,
  scoped validation via `KeyedFormResolver`/`KeyedFormScopeOf`,
  `KeyedFormList` by-id list editing, `KeyedFormSnapshot`.
