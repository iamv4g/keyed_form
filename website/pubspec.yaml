name: website
description: Landing page for keyed_form built with Jaspr
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  jaspr: ^0.23.4
  universal_web: ^1.1.1+1

dev_dependencies:
  build_runner: ^2.10.0
  build_web_compilers: '>=4.0.0 <4.8.6'
  jaspr_builder: ^0.23.4
  lints: ^6.0.0

jaspr:
  mode: static
