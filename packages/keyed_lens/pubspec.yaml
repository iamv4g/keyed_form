name: keyed_lens
description: >-
  Composable total and affine lenses for immutable aggregates, each carrying a
  stable, serializable FieldKey identity for addressing fields across errors,
  focus and patches.
version: 0.1.0
publish_to: 'none'

homepage: https://github.com/iamv4g/keyed_form
repository: https://github.com/iamv4g/keyed_form/tree/main/packages/keyed_lens
issue_tracker: https://github.com/iamv4g/keyed_form/issues

environment:
  sdk: ^3.10.0

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
